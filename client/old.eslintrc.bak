const _ = require('lodash');
const common = require('../eslint.common');

module.exports = _.defaultsDeep(
  {
    env: _.merge(common.env, {
      browser: true,
    }),
    extends: _.union(['plugin:react/recommended'], common.extends),
    parserOptions: _.merge(common.parserOptions, {
      ecmaFeatures: {
        jsx: true,
      },
    }),
    plugins: _.union(common.plugins, ['react']),
    settings: {
      react: {
        version: 'detect',
      },
    },
    rules: {
      'react/react-in-jsx-scope': 'off',
    },
  },
  common
);
